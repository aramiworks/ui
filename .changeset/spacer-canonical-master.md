---
"@aramiworks/ui": patch
---

Spacer: canonicalize Figma master in unified design system file b79qv459pnXaypgNQfNXuc. COMPONENT_SET Spacer (642:4) reduced from 2 Direction variants (horizontal/vertical) to a single COMPONENT named Type=Default (642:2, 16×16). Direction is runtime-only — code's `direction` prop handles axis at render time via `width`/`height` on a `View`. Documentation page (379:80) rebuilt to doc-stack convention with Overview/Variants/Anatomy/Specs FRAMEs — Anatomy switched from RECTANGLE/VECTOR primitives to Card outlined focal + two filled Card boxes flanking a Spacer instance with 3 Connectors, Specs uses a single Table instance (Rows=5) covering Spacing scale / Direction / Flex / Default size / Token source. Story Figma URLs repointed to specific doc nodes (1637:3, 1637:9, 1638:2, 1639:2) and the COMPONENT_SET (642:4).
