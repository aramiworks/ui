---
"@aramiworks/ui": patch
---

FormField: build canonical Figma master at node `1452:2` in unified design system file. Single COMPONENT (no variant axis since all variation is optional content). Composes the canonical TextField atom as an instance for the input. Hidden slots for label, support text, and character count. Text colors bound to `onSurfaceVariant`. Story Figma URLs repointed to new master.
